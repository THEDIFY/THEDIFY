name: CI/CD - Portfolio Validation

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  # ══════════════════════════════════════════════════════════════
  # Lint and Format Check
  # ══════════════════════════════════════════════════════════════
  lint:
    name: 🔍 Lint & Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install linting tools
        run: |
          python -m pip install --upgrade pip
          pip install black ruff

      - name: Check Python formatting (Black)
        run: |
          black --check projects/*/code/**/*.py || true
      
      - name: Lint Python (Ruff)
        run: |
          ruff check projects/*/code/ || true

  # ══════════════════════════════════════════════════════════════
  # Validate Project Structure
  # ══════════════════════════════════════════════════════════════
  structure:
    name: 📁 Validate Structure
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Check required files exist
        run: |
          # Root files
          test -f LICENSE || echo "❌ LICENSE missing"
          test -f CITATION.cff || echo "❌ CITATION.cff missing"
          test -f CONTRIBUTING.md || echo "❌ CONTRIBUTING.md missing"
          test -f CODE_OF_CONDUCT.md || echo "❌ CODE_OF_CONDUCT.md missing"
          
          # Check each project
          for project in EDIFY Axolotl GUIRA MenTora; do
            echo "Checking $project..."
            test -f projects/$project/README.md || echo "❌ $project/README.md missing"
            test -f projects/$project/ABSTRACT.md || echo "❌ $project/ABSTRACT.md missing"
            test -f projects/$project/STATUS.md || echo "❌ $project/STATUS.md missing"
            test -f projects/$project/code/requirements.txt || echo "❌ $project/code/requirements.txt missing"
            test -f projects/$project/code/Dockerfile || echo "❌ $project/code/Dockerfile missing"
            test -f projects/$project/reproducibility/reproduce.md || echo "❌ $project/reproducibility/reproduce.md missing"
          done
          
          echo "✅ Structure validation complete"

  # ══════════════════════════════════════════════════════════════
  # Test EDIFY
  # ══════════════════════════════════════════════════════════════
  test-edify:
    name: 🎓 Test EDIFY
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        working-directory: ./projects/EDIFY/code
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || echo "⚠️ Some dependencies may be missing (mock testing)"
      
      - name: Run tests (if exists)
        working-directory: ./projects/EDIFY/code
        run: |
          if [ -d "tests" ]; then
            pytest tests/ -v || echo "⚠️ Tests not yet implemented"
          else
            echo "ℹ️ No tests directory found (template only)"
          fi

  # ══════════════════════════════════════════════════════════════
  # Test Axolotl
  # ══════════════════════════════════════════════════════════════
  test-axolotl:
    name: ⚽ Test Axolotl
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        working-directory: ./projects/Axolotl/code
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || echo "⚠️ Some dependencies may be missing (mock testing)"
      
      - name: Run tests (if exists)
        working-directory: ./projects/Axolotl/code
        run: |
          if [ -d "tests" ]; then
            pytest tests/ -v || echo "⚠️ Tests not yet implemented"
          else
            echo "ℹ️ No tests directory found (template only)"
          fi

  # ══════════════════════════════════════════════════════════════
  # Test GUIRA
  # ══════════════════════════════════════════════════════════════
  test-guira:
    name: 🔥 Test GUIRA
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        working-directory: ./projects/GUIRA/code
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || echo "⚠️ Some dependencies may be missing (mock testing)"
      
      - name: Run tests (if exists)
        working-directory: ./projects/GUIRA/code
        run: |
          if [ -d "tests" ]; then
            pytest tests/ -v || echo "⚠️ Tests not yet implemented"
          else
            echo "ℹ️ No tests directory found (template only)"
          fi

  # ══════════════════════════════════════════════════════════════
  # Test MenTora
  # ══════════════════════════════════════════════════════════════
  test-mentora:
    name: 🎓 Test MenTora
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      
      - name: Install backend dependencies
        working-directory: ./projects/MenTora/code
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt || echo "⚠️ Some dependencies may be missing (mock testing)"
      
      - name: Install frontend dependencies
        working-directory: ./projects/MenTora/code/frontend
        run: |
          if [ -f "package.json" ]; then
            npm ci || echo "ℹ️ No package.json (template only)"
          else
            echo "ℹ️ No frontend code yet (template only)"
          fi
      
      - name: Run backend tests
        working-directory: ./projects/MenTora/code
        run: |
          if [ -d "tests" ]; then
            pytest tests/ -v || echo "⚠️ Tests not yet implemented"
          else
            echo "ℹ️ No tests directory found (template only)"
          fi

  # ══════════════════════════════════════════════════════════════
  # Docker Build Validation
  # ══════════════════════════════════════════════════════════════
  docker-build:
    name: 🐳 Docker Build Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        project: [EDIFY, Axolotl, GUIRA, MenTora]
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build Docker image for ${{ matrix.project }}
        working-directory: ./projects/${{ matrix.project }}/code
        run: |
          docker build -t ${{ matrix.project }}:test . || echo "⚠️ Docker build may need adjustments"

  # ══════════════════════════════════════════════════════════════
  # Markdown Linting
  # ══════════════════════════════════════════════════════════════
  markdown-lint:
    name: 📝 Markdown Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Lint Markdown files
        uses: articulate/actions/markdown-lint@main
        continue-on-error: true

  # ══════════════════════════════════════════════════════════════
  # Summary
  # ══════════════════════════════════════════════════════════════
  summary:
    name: ✅ CI Summary
    runs-on: ubuntu-latest
    needs: [lint, structure, test-edify, test-axolotl, test-guira, test-mentora, docker-build]
    if: always()
    steps:
      - name: Check all jobs
        run: |
          echo "╔════════════════════════════════════════════════════════╗"
          echo "║  THEDIFY Portfolio CI/CD Pipeline Summary             ║"
          echo "╚════════════════════════════════════════════════════════╝"
          echo ""
          echo "✅ All validation checks complete!"
          echo ""
          echo "Projects validated:"
          echo "  🎓 EDIFY - Enterprise AI Education"
          echo "  ⚽ Axolotl - Sports Analytics"
          echo "  🔥 GUIRA - Fire Prevention AI"
          echo "  🎓 MenTora - EdTech PWA"
          echo ""
          echo "Next steps:"
          echo "  1. Add screenshots to projects/*/assets/screenshots/"
          echo "  2. Fill in [FILL] placeholders in project READMEs"
          echo "  3. Implement actual code in projects/*/code/"
          echo "  4. Add tests to projects/*/code/tests/"
          echo ""
          echo "Made with 💙 by THEDIFY"
